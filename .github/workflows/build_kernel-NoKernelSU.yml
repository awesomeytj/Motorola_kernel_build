name: Build Kernel (nio, Droidspaces, no KernelSU)


on:
  workflow_dispatch:


jobs:
  build-kernel:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Setup environment
        run: |
          echo "ANYKERNEL_SOURCE=$(cat config.env | grep -w "ANYKERNEL_SOURCE" | head -n 1 | cut -d "=" -f 2)" >> $GITHUB_ENV
          echo "ANYKERNEL_SOURCE_BRANCH=$(cat config.env | grep -w "ANYKERNEL_SOURCE_BRANCH" | head -n 1 | cut -d "=" -f 2)" >> $GITHUB_ENV
          echo "KERNEL_SOURCE=$(cat config.env | grep -w "KERNEL_SOURCE" | head -n 1 | cut -d "=" -f 2)" >> $GITHUB_ENV
          echo "KERNEL_SOURCE_BRANCH=$(cat config.env | grep -w "KERNEL_SOURCE_BRANCH" | head -n 1 | cut -d "=" -f 2)" >> $GITHUB_ENV
          echo "KERNEL_CONFIG=$(cat config.env | grep -w "KERNEL_CONFIG" | head -n 1 | cut -d "=" -f 2)" >> $GITHUB_ENV
          echo "BUILD_ARGS=$(cat config.env | grep -w "BUILD_ARGS" | head -n 1 | cut -d ":" -f 2)" >> $GITHUB_ENV
          sudo apt-get update
          sudo apt-get install -y libselinux-dev
          # The prebuilt Snapdragon-LLVM clang links against libtinfo.so.5 /
          # libncurses.so.5 (ncurses 5). The runner image moved to ncurses 6 and
          # dropped libncurses5/libtinfo5, so `clang -v` fails with
          # "libtinfo.so.5: cannot open shared object file". Fetch the .deb from
          # the Ubuntu pool (name resolved at runtime to survive version bumps).
          # python2 is NOT needed: gcc-wrapper.py (its only consumer) is bypassed
          # because BUILD_ARGS sets CC=clang.
          POOL="http://security.ubuntu.com/ubuntu/pool/universe/n/ncurses"
          for pkg in libtinfo5 libncurses5; do
            deb=$(curl -sSL "$POOL/" | grep -oE "${pkg}_[^\"]*_amd64\.deb" | sort -V | tail -n1)
            echo "Fetching $deb"
            curl -sSLO "$POOL/$deb"
          done
          sudo dpkg -i libtinfo5_*_amd64.deb libncurses5_*_amd64.deb
      - name: Pull toolchain
        run: |
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 aarch64-linux-android-4.9
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9 arm-linux-androideabi-4.9
          git clone https://github.com/xiangfeidexiaohuo/Snapdragon-LLVM.git clang
          
      - name: Pull kernel source
        run: |
          git clone --depth=1 ${{ env.KERNEL_SOURCE }} -b ${{ env.KERNEL_SOURCE_BRANCH }} kernel-source

      - name: Add Droidspaces kernel config
        run: |
          cp "$GITHUB_WORKSPACE/droidspaces.config" kernel-source/arch/arm64/configs/droidspaces.config

      - name: Build kernel
        run: |
          export KBUILD_BUILD_USER="hiahia"
          export PATH=$GITHUB_WORKSPACE/clang/bin:${PATH}
          args="                  ARCH=arm64 \
                                  O=../out \
                                  ${{ env.BUILD_ARGS }} \
                                  CLANG_TRIPLE=aarch64-linux-gnu- \
                                  CROSS_COMPILE=$GITHUB_WORKSPACE/aarch64-linux-android-4.9/bin/aarch64-linux-android- \
                                  CROSS_COMPILE_ARM32=$GITHUB_WORKSPACE/arm-linux-androideabi-4.9/bin/arm-linux-androideabi- "
          cd kernel-source
          clang -v
          # Merge device defconfig + Droidspaces fragment; olddefconfig resolves deps.
          make ${args} ${{ env.KERNEL_CONFIG }} droidspaces.config
          make -j$(nproc --all) ${args}

      - name: Verify Droidspaces config (post-olddefconfig)
        run: |
          # The build wrote the final resolved .config to out/.config (O=../out
          # from kernel-source == out/ from repo root). Confirm olddefconfig did
          # not silently drop symbols we care about.
          cfg=out/.config
          echo "=== Droidspaces-relevant symbols in $cfg ==="
          fail=0
          for sym in USER_NS IKCONFIG IKCONFIG_PROC NAMESPACES; do
            line=$(grep -E "^(CONFIG_${sym}=|# CONFIG_${sym} is not set)" "$cfg" || true)
            if echo "$line" | grep -q "=y"; then
              echo "[OK]   $line"
            else
              echo "[MISS] CONFIG_${sym} -> ${line:-not present}"
              fail=1
            fi
          done
          if [ "$fail" -ne 0 ]; then
            echo "::warning::One or more expected CONFIG symbols were dropped by olddefconfig (see above)."
          fi

      - name: Package kernel
        run: |
          git clone --depth=1 ${{ env.ANYKERNEL_SOURCE }} -b ${{ env.ANYKERNEL_SOURCE_BRANCH }} AnyKernel3 && rm -rf AnyKernel3/.git AnyKernel3/.github AnyKernel3/LICENSE AnyKernel3/README.md
          # Replace the stock ExampleKernel (Galaxy Nexus) anykernel.sh with our
          # nio-specific config, otherwise the flasher aborts with
          # "Unsupported device" and never patches boot.
          cp "$GITHUB_WORKSPACE/anykernel.sh" AnyKernel3/anykernel.sh
          if [[ -f out/arch/arm64/boot/Image.gz-dtb ]]; then
            cp out/arch/arm64/boot/Image.gz-dtb AnyKernel3/Image.gz-dtb
          elif [[ -f out/arch/arm64/boot/Image-dtb ]]; then
            cp out/arch/arm64/boot/Image-dtb AnyKernel3/Image-dtb
          elif [[ -f out/arch/arm64/boot/Image.gz ]]; then
            cp out/arch/arm64/boot/Image.gz AnyKernel3/Image.gz
          elif [[ -f out/arch/arm64/boot/Image ]]; then
            cp out/arch/arm64/boot/Image AnyKernel3/Image
          fi
          if [ -f out/arch/arm64/boot/dtbo.img ]; then
            cp out/arch/arm64/boot/dtbo.img AnyKernel3/dtbo.img
          fi

      - name: Upload kernel to artifact
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3_droidspaces_nokernelsu
          path: AnyKernel3/*

      - name: Upload resolved .config to artifact
        uses: actions/upload-artifact@v4
        with:
          name: kernel_config_droidspaces
          path: out/.config
          if-no-files-found: warn
